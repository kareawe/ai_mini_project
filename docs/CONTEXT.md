# CONTEXT

- Project scope stays intentionally small: LangGraph-based CLI workflow only.
- Existing workflow must remain: Supervisor -> Company Discovery -> Web Search -> Report -> Formatting.
- Search data persists to JSON and is reused in report-only mode.
- Readability is preferred over abstraction-heavy design.
